const {modpacks} = require('./modpack')

/**
 * On application restart we need to recreate every modpack object. This does that.
 */
const recreateModpacks = function() {
}


module.exports = {recreateModpacks}